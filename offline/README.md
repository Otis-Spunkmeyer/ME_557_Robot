# ME557 Offline (Arduino)

This folder is the offline workspace.

Key paths:

- `arduino/` firmware and trajectory header.
- `tools/` export and calibration scripts.
- `individualMove.ino` helper sketch.

Entry guide:

- `arduino/README.md`
